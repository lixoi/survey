1. kubectl api-resources - показывает ресурс пренадлехит namespace мли нет // nodes - тоже ресурс, но resource white
2. Параметры запуска kube-apiserver - получение заданных параметроыв аутентиффикации: ps aux | grep kube-apiserver | grep anonymus 
	Длжен быть отключен анонимный режим, basic-аутентификация (минимум)
3. На мастере vim /etc/kubernetes/manifests/kube-apiserver.yaml


Вопросы:
В кубе нет сущностей, связанных с пользователями (типа детали пользователя в LDAP, например) - т.е. понятие пользователя есть, но стореджа, где это хранится, нет
ServiceAccounts -внутренние пользователи куба


